services:
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend/datos:/app/datos
      - ./backend/model_data:/app/model_data
      - ./frontend:/app/frontend